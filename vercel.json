{
  "version": 2,
  "installCommand": "cd namaste_icd_mapper && npm install",
  "buildCommand": "cd namaste_icd_mapper && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
